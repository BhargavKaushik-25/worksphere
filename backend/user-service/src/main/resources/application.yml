spring:
  application:
    name: ${USER_SERVICE_NAME:user-service}

server:
  port: ${USER_SERVICE_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: ${USER_ACTUATOR_ENDPOINTS:health,info}
  endpoint:
    health:
      probes:
        enabled: ${USER_ACTUATOR_PROBES:true}

logging:
  level:
    root: ${USER_LOG_LEVEL:INFO}
